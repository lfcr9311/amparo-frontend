stages:
  - test
  - deploy

npm-test:
  image: node:18-alpine
  stage: test
  script:
    - npm ci
    - npm test

Docker Run:
  image: sleavely/node-awscli:18.x
  stage: deploy
  script:
    - npm run ci
    - npm run prod
  only:
    - main
    - dev
